package pruebaMock;

import pruebaMock.persistance.entity.Player;
import pruebaMock.persistance.entity.repository.PlayerRepositoryImpl;
import pruebaMock.service.PlayerServiceImpl;

public class Main
{
    public static void main(String[] args) {
        /*
        PlayerRepositoryImpl playerRepository = new PlayerRepositoryImpl();

        PlayerServiceImpl playerService = new PlayerServiceImpl(playerRepository);

        System.out.println(playerService.findAll());

        System.out.println(playerService.findById(1L));

        playerService.deleteById(1L);
        System.out.println(playerService.findAll());

        Player player = new Player(5L, "Luis Díaz", "Manchester", "Defensor");

        playerService.save(player);
        System.out.println(playerService.findAll());
        */


    }
}
